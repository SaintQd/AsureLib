name: AsureLib
version: '1.0'
author: SaintQd
main: org.saintqd.asurelib.AsureLib
description: AsureLib Plugin
api-version: '26.2'
dependencies:
  server:
    Vault:
      load: BEFORE
      required: false
    PlaceholderAPI:
      load: BEFORE
      required: false
    DiscordSRV:
      load: BEFORE
      required: false
    MythicMobs:
      load: BEFORE
      required: false